// config.js example - copy this file locally and rename it as config.js
// then put your values in. config.js will be gitignored
const config = {
    app: {
        port: 12345
    },
    db: {
        host: 'localhost',
        user: 'username',
        password: 'password',
        database: 'database'
    }
};

module.exports = config;
// gfjsbdpaoigfnidauhsvnoW