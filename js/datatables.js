console.log("we're here");

// Cat - most of your new work will go here, to build a string that is html, add that using innerHTML =

$(document).ready( function () {
    $('#flightsTable').DataTable();
    } );